# 引き継ぎ

> このファイルは**別のAIエージェント（Codex など）が読んで作業を継続する**ことを想定している。
> 会話の文脈を知らない前提で、必要な事実をすべてここに書く。
> 作業を進めたら**このファイルも更新すること。**

最終更新: 2026-09-23（PR #14 の朝の誤警告修正を整理）

## PR #14: 朝の誤警告を止める

- PR: https://github.com/hide10/steps-log/pull/14 。歩数センサーは未歩行時にイベントを返さないため、生ログの最終時刻だけで計測停止を判定すると朝に誤警告が出る。
- 「前回ワーカーが動いてから12時間」の判定も外した。ワーカーが止まっている間には通知できず、再開後の実行間隔だけでは読み取り失敗を証明できない。`lastSyncAt` の保存も不要になった。
- この PR では身体活動の権限が無く、Health Connect も使えない場合や、両ソースが利用できない場合の警告を残す。実際の計測停止を見分ける診断は別作業で設計し直す。
- Android `assembleDebug testDebugUnitTest` は 231 件 / 失敗 0。実機への反映は、常駐記録を含む後続 PR #17 と合わせて確認する。PR #14 単体の APK を入れると、現在の常駐記録を戻してしまうため。

> **issue 番号について。** このリポジトリ（`steps-log`）は 2026-09-03 に
> 履歴を引き継がずに作り直したので、issue 番号は #1 から振り直されている。
> コミットメッセージに出てくる #8 / #21 / #23 / #24 などは**乗り換え前の
> 別リポジトリの番号**で、いまの issue とは対応しない。

## 引き継ぐ人へ（最初に読む）

**実装の残件は無い。** 機能は本家 StepsApp と同等以上まで来ていて、
v1.1.1 を公開済み。open issue は #1（健康データの生サンプル）だけで、
それも「しばらく使ってから判断する」で意図的に止めてある。

だから次にやることは、**使ってみて気づいたことを直す**のが中心になる。
実際、直近の変更はほとんどが実機で使って出た不満から来ている
（歩数が半分に見える / 切り替えが重い / 戻るボタンが無い / 連続の数字が読めない）。

進め方で守ってほしいことが3つある。

1. **画面や見た目を伴うものは、実装前に案を出して選んでもらう。**
   何度も作り直しになった反省から決めたこと
2. **「壊してはいけない不変条件」を読んでから触る。** ここは壊れても
   エラーにならず、静かに間違った歩数が出る。テストで固定してある
3. **コード変更のあとは `./dev.sh`。** ビルド→テスト→実機インストールまで通る。
   ユーザーは毎日この端末で使っていて、そこで気づいたことが次の改善になる

## これは何か

`steps.app`（歩数計アプリ。週/月/年の平均表示や傾向分析は PRO 課金）の自作版。
**目標は「StepsApp に勝るとも劣らない俺専用の歩数計アプリ」**（ユーザーの言葉）。
歩数を課金アプリに預けず、自分の SQLite と git に履歴を持ち続けるのが狙い。

コメントとコミットメッセージは日本語で書く。

## 構成

```
<このリポジトリ>/
├── android/                    Kotlin 2.1 + Compose + Room + WorkManager + Health Connect
├── server/                     Bun + Hono + bun:sqlite（PC 側ダッシュボード）
├── dev.sh                      ビルド→テスト→実機インストール を1コマンドで
├── AGENTS.md                   設計方針。**着手前に必読**
└── deploy/                     systemd unit と Caddy 設定

別リポジトリ（任意）             取り込んだ steps.json を履歴として残したい場合の置き場
```

- ダッシュボード（任意）: `https://<好きなホスト名>`（`steps-app.service`、ポート 8430）

## 作業のしかた

```bash
# これ1つでビルド→テスト→実機反映まで通る（推奨）
./dev.sh

# 個別にやる場合
cd android
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
ANDROID_HOME=<Android SDK のパス> ./gradlew assembleDebug testDebugUnitTest

cd server && bun test && bun src/server.ts   # :8430
```

**ユーザーからの常設依頼: ビルドが新しくなったら実機にも入れ直すこと。**
毎日使って気づいたことを次の改善に回す（PDCA）ため。`./dev.sh` がそれをやる。

### 実機・エミュレータ

- 実機のシリアルは `dev.env` に書く（`DEVICE_SERIAL`）
- adb は **Windows 側**を使う:
  `/mnt/c/Users/<あなた>/AppData/Local/Android/Sdk/platform-tools/adb.exe`
- **Windows の adb は出力に CR を混ぜる。** `tr -d '\r'` してから判定すること
- エミュレータが同時に繋がっていることがあるので `-s <serial>` で対象を明示する
- 端末の DB を覗く:
  ```bash
  # adb shell はバイナリを壊すので exec-out。端末に sqlite3 は無い。
  # Room は WAL なので -wal も取らないとテーブルが空に見える
  for f in steps.db steps.db-wal steps.db-shm; do
    $ADB -s $SERIAL exec-out "run-as app.stepsapp cat databases/$f" > /tmp/d.${f#steps.db}
  done
  ```
- エミュレータにテストデータを入れるときは、**Room が作った DB を pull して
  sqlite3 で行を足し、push し返す**（identity_hash が保たれる）。
  push 先は `/sdcard` だと run-as から読めないので `/data/local/tmp` に置いて chmod 666。

### 規約

- **main に直接コミットしない。** フィーチャーブランチ + PR
- **`gh` コマンドは Haiku サブエージェントに委譲する**（Claude の運用ルール。
  Codex が引き継ぐ場合はこの限りではないが、`gh` の出力は冗長なので注意）
- git のローカル操作は直接やってよい
- コミットメッセージは日本語。**なぜそうしたか**を書く

## 壊してはいけない不変条件

ここを壊すと「明らかに動かない」のではなく**静かに間違った歩数が出る**。
必ずテストで固定してある。触るときはテストを読むこと。

1. **センサーと Health Connect を合算しない。**
   1日の採用値は必ずどちらか一方。**そのうえで大きいほうを採る**
   （2026-08-31 変更。以前は HC 固定優先だったが、HC が「読めるが少ない」日に
   表示が半分になった。同数なら HC）。
   → `domain/SourceSelection.kt`
2. **歩数は1日のなかで減らない。**
   小さい値での上書きは「信頼できるソースが一時的に読めなかった」ことを意味する。
   → `shouldReplaceDay()`。実機で歩数が消えるバグを踏んで追加した
3. **週の集計に `strftime('%Y-%W')` を使わない。**
   年をまたぐ週が分断される（2026-12-28(月)〜2027-01-03(日) が 2026-52 と 2027-00 に割れる）。
   `date(d, '-' || ((strftime('%w', d) + 6) % 7) || ' days')` で「その週の月曜日」を求める。
   `date(d,'weekday 1','-7 days')` も月曜日そのものを1週ずらすので不可。
   → Kotlin 側 `weekStart()` と SQL の一致を 1200 日ぶん突き合わせる
     `WeekStartCrossCheckTest` がある。**両方を同時に直すこと**
4. **「レコードが無い日 = 未計測」と「0歩の記録 = 実際に0歩」を厳密に区別する。**
   平均の分母は記録がある日だけ。ストリークは未計測では切れず、0歩では切れる
5. **センサーの初回読み取りは基準を作るだけで0歩から始める。**
   TYPE_STEP_COUNTER は再起動以来の累積値なので、そのまま計上してはいけない
6. **連続日数は「途切れずに達成した日数」だけを意味する。**
   一度「月2回まで未達の日を守る」機能を入れたが、**撤回した**（2026-09-03）。
   守った日を数えない作りにしたところ、カレンダー上では6日つながって見えるのに
   「連続4日」と出て、どう数えているのか読めなかった。
   **数え方が一つであることを優先する。復活させないこと。**
7. **達成の判定は「その日に有効だった目標」で行う。**
   目標をひとつしか持たないと、目標を変えた瞬間に過去の達成が塗り替わり、
   連続日数の自己ベストまで書き換わる。判定は必ず `GoalHistory` を通す。
   → `domain/GoalHistory.kt`、`GoalHistoryTest`

## 実装済み

- 歩数の記録（センサー + Health Connect、WorkManager 15分間隔、FGS なし）
- エクスポート/インポート（JSON=完全バックアップ / CSV=分析用、衝突解決3モード）
- GitHub private repo への日次アップロード（PAT は EncryptedSharedPreferences）
- 目標設定と達成リング、ストリーク（現在・自己ベスト）
- **目標の履歴**（#23）。達成判定・ストリーク・カレンダーは
  **その日に有効だった目標**で行う。目標を上げても過去の達成は消えない
- アプリ内の 日/週/月/年 切り替えと集計、前期間との比較
- **歩いた時間帯**（#24）。ホームの「日」に24時間の帯グラフ。
  Health Connect の1時間集計を優先し、読めなければ生ログの差分で近似する
- 距離・消費カロリーの推定（体重未設定ならカロリーは出さない）
- ホーム画面ウィジェット（Glance）
- SNS 共有用の画像生成（#21）。既定は歩数と日付のみ。プレビューは共有される画像そのもの
- PC 側ダッシュボード（週/月/年の平均、SVG グラフ、CSV 配信）
- **バックアップ先は Google ドライブに統一**（GitHub 方式は削除済み）
- テーマとアクセントカラー（6色 + ライト/ダーク）。Web ビューアと同じ色値
- **サーバレス Web ビューア**（`web/`）。常駐サーバ不要で、
  データがブラウザから出ない
- **体重・睡眠の取り込み**（Health Connect）。本家 StepsApp に無い機能
- **自己記録**。連続の画面に1日の最高・週/月の最高平均・最長の連続・累計。
  更新したときは通知とホームで知らせる。比べる相手は常に「過去の自分」
- **年ごとの記録**。年ごとの1日あたり平均・その年の最高・記録日数。
  記録がある年に集中して見える理由を画面から読めるようにするためのもの
- **週のまとめ**。月曜の朝8時以降に通知する。「7日中3日の記録」のように
  分母を必ず添える。アプリ内に履歴は持たない
- **共有画像**。中身を中央に積む構成。背景は柄10種と写真14枚（写真は
  ローカルの画像生成で作ったもの。`res/drawable-nodpi/share_bg_*.webp`）。
  載せる項目の雛形3種

テスト: Android 233 件 / server 30 件 / web 29 件、いずれも全通過。
`./dev.sh` で全部回る。

## 本家 StepsApp との機能差

**機能面はすべて埋まっている。** 目標リング / ストリーク / 日週月年 /
距離・カロリー / グラフ / 前期間比較 / ウィジェット / ウェアラブル(HC経由) /
テーマ / エクスポート。
チャレンジ・ランキング等のソーシャル機能は**意図的に対象外**（下記）。

## 未実装・次にやること

GitHub issue を見ること。

### バックアップ先のフォルダ選択

**Google Cloud の登録は不要。** OAuth 方式をやめ、標準のフォルダ選択
（`ACTION_OPEN_DOCUMENT_TREE`）でクラウドのフォルダに直接書く方式にした。
Google ドライブの DocumentsProvider が `FLAG_SUPPORTS_CREATE` を立てていることを
実機で確認している。当初 Drive API + OAuth で実装したが、Cloud プロジェクトの用意が
要って実運用に入れられず、方式ごと差し替えた。

- 選んだフォルダは `shared_prefs` に `folder_uri`
  （`content://com.google.android.apps.docs.storage/tree/...`）として保存される
- 書き出されるのは `steps.json` と、Web ビューアの `index.html`

**アップロードは日次なので、当日ぶんはまだ載っていないのが正常。**
その日のうちに反映したいときは、アプリの設定から「いますぐアップロード」を叩く。
同じ理由で「クラウドの値が端末より小さい」ことも起きる（アップロード後に
不変条件2で大きい値へ上書きされた場合）。差分を見て慌てないこと。

### そのほか

- **#1 健康データ（保留中）**: 体重・睡眠に加え、心拍・血圧・体脂肪率・血中酸素・
  運動時間なども読み取り/保存/表示/バックアップまで通っている
  （`vitals` テーブル、1日1レコードの代表値）。
  生の全サンプルは持たない方針（心拍だけで1日数千件になり、端末にも書き出し先にも重い）。
  **「しばらく使ってから判断する」で止めてある。** 急いで着手しないこと
- **Web ビューアは目標の履歴に未対応。** 自分で目標を選ぶ作りのまま
  （`steps.json` には履歴が入っているので、読もうと思えば読める）
- **Web をスマホのブラウザで開けるようにするか** は未決着

## リリース運用

**配布は GitHub Releases。** 手元で release ビルドして APK を添付する。

```bash
cd android
ANDROID_HOME=<Android SDK> ./gradlew assembleRelease
# app/build/outputs/apk/release/app-release.apk を steps-log-<版>.apk にして添付
gh release create v1.2.0 --target main --title "v1.2.0" --notes-file <ノート> <APK>
```

- バージョンは `android/app/build.gradle.kts` の `versionCode` / `versionName`。
  設定画面の末尾にも出している
- **署名鍵は `~/steps-app-keys/`**（`steps-app.jks` と `keystore.properties`、権限 700）。
  リポジトリには入れない。`assembleRelease` はリポジトリ直下か
  `~/steps-app-keys/` から `keystore.properties` を探し、無ければ署名なしでビルドする
- **鍵を失うと更新版を配れなくなる**（別の鍵の APK は上書きインストールできず、
  端末で一度アンインストール＝記録が消える）。OneDrive にもバックアップ済み
- **バージョンアップはある程度まとめる**（ユーザーの方針）。
  機能ひとつごとに出さず、いくつか溜めてから1つの版にする

### いまの状況（2026-09-05）

- 公開済みは **v1.1.1**
- **`main` には「年ごとの記録」が入っているが、まだリリースしていない。**
  次の変更が溜まったら versionCode 5 / versionName 1.2.0 として出す
- **実機には debug ビルドが入っている。** release 版に入れ替えない、という判断
  （`isMinifyEnabled = false` なので実質の差は署名だけで、動作もデータも同じ。
  入れ替えるとアンインストールが必要で記録が消えるため、揃える価値がない）

## ユーザーが決めたこと（勝手に変えない）

- **他人と比べる・争う機能は作らない。** チャレンジ / チーム / ランキング /
  フレンド / チャットは対象外。一人用アプリなので前提と衝突する。
  競争が欲しければ「過去の自分と比べる」形に読み替える
- **本家 StepsApp との歩数の一致は目指さない。**
  2026-08-25 時点で 自作 3,480 / 本家 3,269（差 約6%）。
  「無理に合わせなくてもいい」との判断。**機能面の同等が目標であって数値の一致ではない**
- **歩数の実データは公開しない。** 歩数は健康かつ行動の記録で、
  在宅/外出のパターンが読み取れる。コードは公開してよいが、データは別扱いにする

## 端末側で残っている設定

- `READ_HEALTH_DATA_IN_BACKGROUND` が未許可。
  設定 → Health Connect → 歩数ログ → バックグラウンドでのデータ読み取りを ON。
  未許可でもデータは壊れない（不変条件2で守っている）が HC ではなくセンサー主体になる
- バッテリー使用量を「制限なし」に（OEM の省電力対策）

## 踏んだ落とし穴

- **`getCurrentDeviceDataSource()` は存在しない。**
  Health Connect の SPN 対策としてこの API を使う前提で計画していたが、
  connect-client 1.1.0 / 1.2.0-alpha05 のどの aar にも無かった。
  `aggregate()` が優先順位リストで重複を自動排除するのでそもそも不要。
  **Web 調査が出した API 名は、実装前に aar を展開して実在を確かめること。**
- connect-client 1.1.0 は compileSdk 36 を要求する。
  そのため Gradle 8.13 / AGP 8.11.1 / compileSdk 36（targetSdk は 35 のまま）
- **「Health Connect が読めない」だけでなく「読めるが少ない」ことがある。**
  実機で、センサーが 10,250 歩を数えている日に Health Connect が 5,045 しか
  返さず、表示が半分のままになった（HC にデータを書く側の同期が遅れていた）。
  当時は HC 固定優先だったため、大きいほうのセンサー値が捨てられていた。
  **1日の値は大きいほうを採る**ように変えた（合算しない原則はそのまま）。
- **画面ごとに同じ言葉で違う計算をしない。** 連続日数を、ホームでは素の数え方、
  連続の画面では別の数え方にしていて、同じ「連続」で違う数字が出ていた。
  **同じ言葉には同じ計算を。**
- **機能を足すときは、それが画面から読めるかまで考える。**
  「連続を守る」機能は、守った日を数えない作りにしたら
  「6日つながって見えるのに連続4日」となって撤回に至った。
  数字の正しさだけでなく、**その数字がどう読まれるか**まで見ること。
- **タブでない画面には戻る手段を必ず置く。** 連続・バックアップ・共有に
  戻るボタンが無く、端末の戻る操作しか手段が無かった（`ui/common/BackBar.kt`）。
- **権限を一度断られたら、ダイアログはもう出ない。** Android の仕様。
  文言だけ出して終わりにすると手詰まりになるので、
  アプリの設定を開く導線を必ず添える。
- **記録が増えると画面が重くなる。** 日週月年の切り替えが引っかかっていた原因は3つで、
  いずれも「毎回すべてやり直す」作りだった（2026-09-01 修正）。
  1. 期間に関係ない計算（ストリーク・体重/睡眠の傾向）まで切り替えのたびに再計算していた
     → 4期間ぶんの集計を記録更新時に作り置きし、切り替えでは取り出すだけにした
  2. 1,200 日ぶんの計算を UI スレッドでやっていた → `Dispatchers.Default` に逃がした
  3. 一覧の全行を `Column` で一度に作っていた（日表示で 1,200 行）
     → 「もっと見る」で少しずつ足す
  `currentStreak()` が遡るたびに全キーを走査していた（二乗）のも直した。
  **画面に出す量と計算量は、記録の増加に引きずられないようにすること。**
- locapin（別プロジェクト）は `keystore.properties` と `*.jks` が git 管理下に入っている。
  **steps-app では最初のコミットから .gitignore 済み。同じ轍を踏まないこと**
